# The `services` package holds business logic that is separate from the
# Flask routing layer (`app.py`). Keeping this logic separate makes the
# project easier to test and maintain, which is standard professional
# Flask architecture.
